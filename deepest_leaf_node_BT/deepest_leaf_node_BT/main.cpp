#include<iostream>
#include <vector>
#include "tree.h"

using namespace std;

int main(){
	tree <int> myTree;
	int num,A[100];
	cin>>num;

	for(int i=0;i<num;i++)
		cin>>A[i];
	myTree.createTree(A,num);
	my
	return 0;
}